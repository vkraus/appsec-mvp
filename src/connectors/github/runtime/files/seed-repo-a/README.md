# Seed repo A — deliberately vulnerable

Python Flask fixture with two canonical defects (CWE-89, CWE-78) positioned so
SonarQube and Semgrep both flag them on the same lines. Used by the thesis MVP
to demonstrate cross-tool deduplication.

**Do not deploy.**
