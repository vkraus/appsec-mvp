# Seed repo B — deliberately vulnerable

Node.js Express fixture with two canonical defects (CWE-79, CWE-22) positioned so
SonarQube and Semgrep both flag them on the same lines. Used by the thesis MVP
to demonstrate cross-tool deduplication.

**Do not deploy.**
