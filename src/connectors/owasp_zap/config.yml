source: owasp_zap
category: dast
# severity_map / status_map are aspirational pointers — these YAMLs do
# not yet exist on disk. No code currently loads this connector's
# config.yml; when the lookups are introduced, drop them at the paths
# below to keep the convention with other connectors.
severity_map: src/connectors/owasp_zap/severity.yml
status_map:   src/connectors/owasp_zap/status.yml
bucket_secret_scope: mvp-connectors
bronze_table: ${catalog}.bronze_owasp_zap.findings
cicd_prefix: cicd/zap/
zap_api_url_secret: zap_url
zap_api_key_secret: zap_api_key
