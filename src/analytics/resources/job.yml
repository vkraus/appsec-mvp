resources:
  jobs:
    analytics:
      name: analytics
      tasks:
        - task_key: gold_findings_summary
          notebook_task:
            notebook_path: ../sql/gold_findings_summary_placeholder.sql
          job_cluster_key: shared
      job_clusters:
        - job_cluster_key: shared
          new_cluster:
            spark_version: "14.3.x-scala2.12"
            node_type_id: Standard_DS3_v2
            num_workers: 1
      # No schedule — operator runs on-demand once analytics implementation lands:
      #   databricks bundle run analytics
