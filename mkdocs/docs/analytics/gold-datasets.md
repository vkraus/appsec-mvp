# Gold datasets

!!! info "Forthcoming"
    Per-dataset pages for the Gold-layer tables and views will be authored
    in a follow-up. The DAB already creates the `gold` schema (see
    `src/analytics/resources/schemas.yml`) so future fragments can be
    dropped in without bundle changes. For now, see
    [Evidence scenarios](evidence.md) for the queries that consume Silver
    directly and [Canonical mapping](../platform/reference/canonical-mapping.md)
    for the Silver contract Gold builds on.
